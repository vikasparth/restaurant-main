[flake8]
max-line-length = 88
# E203: whitespace before ':' — conflicts with black slice formatting
# E501: line too long — black handles line length; SQL/HTML strings are exempt
extend-ignore = E203, E501
exclude = venv
