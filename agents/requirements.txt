# ============================================================
# Agent Dependencies — Aap ki Rasoi
# Install with: pip install -r requirements.txt
# Run inside agents/.venv — do not install into the backend venv
# ============================================================

# Anthropic SDK — agentic loop, tool use, prompt caching
anthropic==0.97.0

# Pydantic — finding schema models, auto-generates finding-schema.json
pydantic==2.13.3

# JSON Schema validation — validates agent findings against finding-schema.json
jsonschema==4.26.0

# GitHub API — read issues, commits, PRs (read-only)
PyGithub==2.9.1

# HTTP client — Sentry API and Render API calls
requests==2.33.1

# YAML parsing — extracts and parses YAML blocks from agent finding comments
pyyaml==6.0.3

# Loads agents/.env into os.environ at import time — skips vars already set in the shell
# so CI/Render env vars are never overridden by the local file
python-dotenv==1.1.0

# Testing
pytest==9.0.3
colorama==0.4.6
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
pygments==2.20.0

# Sentry SDK — agent observability, Performance transactions for token usage and confidence tracking
sentry-sdk==2.58.0
certifi==2025.4.26
urllib3==2.4.0


